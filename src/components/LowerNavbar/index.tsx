import React from "react";
import { useStyles } from "./style";

const LowerNavbar = () => {
  const classes = useStyles();
  return <div className={classes.container}>LowerNavbar</div>;
};

export default LowerNavbar;
